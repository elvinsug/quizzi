<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quizzi"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".activities.JoinGameActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LobbyActivity" />
        <activity android:name=".activities.QuestionActivity" />
        <activity android:name=".activities.WaitingResultActivity" />
        <activity android:name=".activities.ScoreRevealActivity" />
        <activity android:name=".activities.RankActivity" />
        <activity android:name=".activities.GameOverActivity" />
    </application>
</manifest>
